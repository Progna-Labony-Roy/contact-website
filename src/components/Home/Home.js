import React from 'react';
import Header from '../Header/Header';
import PageInfo from '../PageInfo/PageInfo';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <PageInfo></PageInfo>
        </div>
    );
};

export default Home;